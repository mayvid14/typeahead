import { FilterWordsPipe } from './filter-words.pipe';

describe('FilterWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
